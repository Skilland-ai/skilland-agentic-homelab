---
description: Review an existing spec-driving stage artifact.
argument-hint: <case_id> <stage_id>
---

Use `spec-driving-orchestrator` and `spec-driving-reviewer`.

Arguments: `$ARGUMENTS`

Load `shared/flows/spec-driving/references/command-contract.md` and execute
`spec-drive-review <case_id> <stage_id>`. Update state from the review verdict.
