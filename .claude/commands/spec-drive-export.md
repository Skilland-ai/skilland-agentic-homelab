---
description: Create or update the spec-driving export index.
argument-hint: <case_id>
---

Use `spec-driving-orchestrator`.

Arguments: `$ARGUMENTS`

Load `shared/flows/spec-driving/references/command-contract.md` and execute
`spec-drive-export <case_id>`. Create `handoffs/export_index.md` without
duplicating artifacts.
